// Render-load helpers for the stats panel. The canonical file lives with the
// guest bundle so the server's vitest suite (the one CI runs) covers it.
export * from "../../server/guest/src/renderLoad";
